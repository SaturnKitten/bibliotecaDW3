{% extends "templates/base.html" %}

{% block content %}
<div x-data="leitorForm()">

  <ol class="breadcrumb mb-2">
    <li class="breadcrumb-item active">{{title}}</li>
  </ol>

  <div class="card">
    <div class="card-header">Cadastro de Leitor</div>

    <div class="card-body">

      <form>

        <div class="form-group col-md-6">
          <label>Nome</label>
          <input class="form-control" x-model="form.nome">
        </div>

        <div class="form-group col-md-6">
          <label>Email</label>
          <input class="form-control" x-model="form.email">
        </div>

        <div class="form-group col-md-6">
          <label>CPF</label>
          <input class="form-control" x-model="form.cpf">
        </div>

        <div class="form-group col-md-6">
          <label>Data de Nascimento</label>
          <input type="date" class="form-control" x-model="form.data_nascimento">
        </div>

        <button type="button" @click="insertForm()" class="btn btn-primary mt-4">Salvar</button>
        <a href="/leitor/manutLeitor" class="btn btn-info mt-4 ml-2">Retornar</a>

      </form>

      <div class="mt-3">
        <template x-if="message">
          <div :class="messageClass" x-text="message"></div>
        </template>
      </div>

    </div>
  </div>

</div>

<script>
function leitorForm() {
  return {
    form: {
      nome: '',
      email: '',
      cpf: '',
      data_nascimento: '',
      removido: false
    },

    message: '',
    messageClass: '',

    async insertForm() {
      try {
        const response = await fetch('/leitor/insertLeitor', {
          method: 'POST',
          headers: {"Content-Type": "application/json"},
          body: JSON.stringify(this.form)
        });

        const result = await response.json();

        if (result.status === "ok") {
          alert("Leitor cadastrado!");
          this.resetForm();
        } else {
          this.message = `Erro: ${result.status}`;
          this.messageClass = 'alert alert-danger';
        }
      } catch (error) {
        this.message = `Erro de conexão: ${error.message}`;
        this.messageClass = 'alert alert-danger';
      }
    },

    resetForm() {
      this.form = {
        nome: '',
        email: '',
        cpf: '',
        data_nascimento: '',
        removido: false
      };
      this.message = false;
    }
  }
}
</script>
{% endblock %}

